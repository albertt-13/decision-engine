/**
 * Insight generado por el AI Analyst — dato de lectura/reporting, sin
 * invariantes propias (mismo espíritu que `SalesSnapshot`).
 */
export interface Insight {
  id: string;
  content: string;
  generatedBy: string;
  createdAt: Date;
}
